-- Replace 'YourCategory' with the actual category you want to filter
SELECT * FROM job_description WHERE category = 'IT';

-- Replace the values in the range with your actual price range
SELECT * FROM job_description WHERE salary BETWEEN 50000.00 AND 80000.00;


-- Replace 'YourCategory' with the actual category you want to filter
SELECT * FROM courses WHERE category = 'Programming';

-- Replace the values in the range with your actual price range
SELECT * FROM courses WHERE course_price BETWEEN 100.00 AND 500.00;

